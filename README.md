# **Task timer Front End**

# Instructions

## Run run project in your local machine env:

1. `npm install`
2. `npm start`

## Run run project in docker env:
1. docker build -t react-fe .
2. docker run -dp 8080:8080 react-fe



